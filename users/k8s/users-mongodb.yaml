apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: users-mongo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-mongodb-deployment # 👈 Уникальное имя
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-mongodb # 👈 Своя метка
  template:
    metadata:
      labels:
        app: users-mongodb
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      containers:
        - name: mongodb
          image: mongo:7.0
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              value: rootadmin
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: supersecretpassword
            - name: MONGO_INITDB_DATABASE
              value: usersdb
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
      volumes:
        - name: mongo-storage
          persistentVolumeClaim:
            claimName: users-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: users-mongodb-service # 👈 DNS-имя для user-service приложения
spec:
  ports:
    - port: 27017
  selector:
    app: users-mongodb
